# Generated by Athena's Deployment Agent.
# Builds a small image that runs the Node backend with `npm start`.
FROM node:22-alpine

WORKDIR /app
ENV NODE_ENV=production PORT=8000

COPY {{ join(d.backend_dir, "package*.json") }} ./
RUN npm install --omit=dev

COPY {{ d.backend_dir or "." }}/ .

EXPOSE 8000
CMD ["npm", "start"]
